name: CI

on:
  push:
    branches: [ main ]
  workflow_dispatch:
  schedule:
    - cron: "0 9 * * 2"

jobs:
  buildx:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
      id-token: write
      attestations: write

    steps:
      - name: Install Cosign
        id: install-cosign
        if: github.event_name != 'pull_request'
        uses: sigstore/cosign-installer@v3.8.2
        with:
          cosign-release: 'v2.4.2'

      - name: Set Build Tag
        id: set-env
        run: |
          echo "TAG=$(date +%Y.%m.%d)" >> $GITHUB_ENV
          echo "TAG is set to $(date +%Y.%m.%d)"

      - name: Docker Login
        id: docker-login
        uses: docker/login-action@v3.4.0
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Login to GitHub Container Registry
        id: github-login
        uses: docker/login-action@v3.4.0
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up QEMU
        id: setup-qemu
        uses: docker/setup-qemu-action@v3.6.0

      - name: Set up Docker Buildx
        id: buildx-setup
        uses: docker/setup-buildx-action@v3.10.0

      - name: Extract Metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: | 
            ${{ github.repository }}
            ghcr.io/${{ github.repository }}
          tags: |
            latest
            ${{ env.TAG }}

      - name: Fix tag formatting for Docker Bake
        id: fix-tags
        run: |
          TAGS="$(echo "${{ steps.meta.outputs.tags }}" | tr '\n' ',')"
          echo "tags=$TAGS" >> $GITHUB_OUTPUT
          LABELS="$(echo "${{ steps.meta.outputs.labels }}" | tr '\n' ',')"
          echo "labels=$LABELS" >> $GITHUB_OUTPUT

      - name: Docker Bake Build and Push
        uses: docker/bake-action@v6.7.0
        with:
          push: true
          sbom: true
          provenance: mode=max
          set: |
            main.tags=${{ steps.fix-tags.outputs.tags }}
            main.labels=${{ steps.fix-tags.outputs.labels }}

      - name: Slim Docker Image for amd64
        id: slim-amd64
        uses: kitabisa/docker-slim-action@v1
        env:
          DSLIM_NEW_LABEL: ${{ steps.meta.outputs.labels }}
          DSLIM_IMAGE_BUILD_ARCH: amd64
          DSLIM_HTTP_PROBE: false
        with:
          target: ${{ github.repository }}:latest
          tag: slim-amd64

      - name: Slim Docker Image for arm64
        id: slim-arm64
        uses: kitabisa/docker-slim-action@v1
        env:
          DSLIM_NEW_LABEL: ${{ steps.meta.outputs.labels }}
          DSLIM_IMAGE_BUILD_ARCH: arm64
          DSLIM_HTTP_PROBE: false
        with:
          target: ${{ github.repository }}:latest
          tag: slim-arm64

      - name: Push Intermediate Slim Images
        run: |
          docker tag ${{ github.repository }}:slim-amd64 ghcr.io/${{ github.repository }}:slim-amd64
          docker tag ${{ github.repository }}:slim-arm64 ghcr.io/${{ github.repository }}:slim-arm64
          docker push ${{ github.repository }}:slim-amd64
          docker push ${{ github.repository }}:slim-arm64
          docker push ghcr.io/${{ github.repository }}:slim-amd64
          docker push ghcr.io/${{ github.repository }}:slim-arm64

      - name: Create and Push Multi-Arch Manifest for Slim Image
        uses: Noelware/docker-manifest-action@1.0.0
        with:
          inputs: ${{ github.repository }}:slim
          tags: ${{ github.repository }}:slim-amd64,${{ github.repository }}:slim-arm64
          push: true

      - name: Create and Push Multi-Arch Manifest for Slim Image for GHCR
        uses: Noelware/docker-manifest-action@1.0.0
        with:
          inputs: ghcr.io/${{ github.repository }}:slim
          tags: ghcr.io/${{ github.repository }}:slim-amd64,ghcr.io/${{ github.repository }}:slim-arm64
          push: true

      - name: Generate artifact attestation
        id: artifacts
        uses: actions/attest-build-provenance@v2
        with:
          subject-name: index.docker.io/${{ github.repository }}
          subject-digest: ${{ steps.build.outputs.digest }}
          push-to-registry: true

      - name: Generate artifact attestation for GHCR
        id: artifacts-ghcr
        uses: actions/attest-build-provenance@v2
        with:
          subject-name: ghcr.io/${{ github.repository }}
          subject-digest: ${{ steps.build.outputs.digest }}
          push-to-registry: true

      - name: Generate SBOM GHCR
        uses: anchore/sbom-action@v0
        with:
          image: ghcr.io/${{ github.repository }}:latest
          format: 'spdx-json'
          output-file: 'sbom.spdx.json'

      - name: Attest SBOM GHCR
        uses: actions/attest-sbom@v2
        with:
          subject-name: ghcr.io/${{ github.repository }}
          subject-digest: ${{ steps.build.outputs.digest }}
          sbom-path: 'sbom.spdx.json'
          push-to-registry: true

      - name: Sign the Published Docker Image
        id: sign-image
        if: github.event_name != 'pull_request'
        env:
          TAGS: ${{ steps.meta.outputs.tags }}
          DIGEST: ${{ steps.build.outputs.digest }}
        run: |
          for tag in $TAGS; do
            echo "Signing ${tag}@${DIGEST}"
            COSIGN_EXPERIMENTAL=1 cosign sign --yes "${tag}@${DIGEST}"
          done

      - name: Sign Slim Image
        id: sign-slim
        if: github.event_name != 'pull_request'
        env:
          COSIGN_EXPERIMENTAL: 1
          SLIM_TAGS: "${{ github.repository }}:slim ghcr.io/${{ github.repository }}:slim"
        run: |
          for tag in $SLIM_TAGS; do
            cosign sign --yes "${tag}"
          done

      - name: Generate Release Notes (Docker & Supply Chain Info)
        if: github.event_name == 'release'
        run: |
          TAG="${{ env.TAG }}"
          REPO="${{ github.repository }}"
          {
            echo "## 🚀 Docker Images for This Release"
            echo ""
            echo "This release provides multi-architecture Docker images (\`amd64\`, \`arm64\`, and \`arm\`) published to both **Docker Hub** and **GitHub Container Registry (GHCR)**."
            echo ""
            echo "---"
            echo ""
            echo "### 🔹 **Docker Hub**"
            echo ""
            echo "> **Note:** Docker Hub is the world's most popular public container registry, but it enforces [anonymous pull limits](https://www.docker.com/increase-rate-limits). If you are automating deployments or have many servers, consider using GHCR below to avoid interruptions."
            echo ""
            echo "**Repository:** \`${REPO}\`  "
            echo "**Tags available:** \`latest\`, \`${TAG}\`"
            echo ""
            echo "**Pull commands:**"
            echo "\`\`\`sh"
            echo "docker pull ${REPO}:latest"
            echo "docker pull ${REPO}:${TAG}"
            echo "\`\`\`"
            echo ""
            echo "---"
            echo ""
            echo "### 🔹 **GitHub Container Registry (GHCR)**"
            echo ""
            echo "> **GHCR** offers higher pull rate limits, strong integration with GitHub, and is recommended for most CI/CD or cloud-native environments."
            echo ""
            echo "**Repository:** \`ghcr.io/${REPO}\`  "
            echo "**Tags available:** \`latest\`, \`${TAG}\`"
            echo ""
            echo "**Pull commands:**"
            echo "\`\`\`sh"
            echo "docker pull ghcr.io/${REPO}:latest"
            echo "docker pull ghcr.io/${REPO}:${TAG}"
            echo "\`\`\`"
            echo ""
            echo "---"
            echo ""
            echo "### 🔒 Supply Chain Security"
            echo ""
            echo "These images are signed and include:"
            echo ""
            echo "- **SLSA Provenance:** Attestations to verify how the image was built."
            echo "- **SBOM:** Software Bill of Materials for full component transparency."
            echo "- **Cosign signatures** for tamper-evidence and trust."
            echo ""
            echo "**How to verify:**"
            echo "\`\`\`sh"
            echo "# Verify image signature"
            echo "cosign verify ghcr.io/${REPO}:${TAG}"
            echo ""
            echo "# Verify SLSA provenance"
            echo "cosign verify-attestation ghcr.io/${REPO}:${TAG}"
            echo ""
            echo "# List SBOM attestations"
            echo "cosign verify-attestation --type sbom ghcr.io/${REPO}:${TAG}"
            echo "\`\`\`"
            echo "See [Sigstore Cosign Docs](https://docs.sigstore.dev/cosign/overview/) for full instructions."
            echo ""
            echo "---"
            echo ""
            echo "#### Need help or want to report an issue?"
            echo "Open an issue on [GitHub](https://github.com/${REPO}/issues)."
          } > release_notes.txt
          
      - name: Create Github release
        id: github-releasing
        uses: ncipollo/release-action@v1.16.0
        with:
          tag: ${{ env.TAG }}
          allowUpdates: true
          makeLatest: true
          bodyFile: release_notes.txt
